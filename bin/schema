#!/usr/bin/perl
use warnings;
use strict;

use Cwd qw(abs_path);
use File::Basename qw(dirname);
BEGIN {
    my $dir = dirname(abs_path($0));
    push @INC, "$dir/../lib";
    push @INC, "$dir/../../Jifty/lib";
}   


use Jifty::Script::Schema;
Jifty::Script::Schema->run();
