#!/usr/bin/perl
use warnings;
use strict;
use File::Basename;

BEGIN {
  my $dir = dirname(__FILE__);
  push @INC, "$dir/../lib";
  push @INC, "$dir/../../Jifty/lib";
}

use Jifty::Script::StandaloneServer;
Jifty::Script::StandaloneServer->run;

1;
