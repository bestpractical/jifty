#!/usr/bin/perl
use warnings;
use strict;
use File::Basename qw(dirname); 

BEGIN {
    my $dir = dirname(__FILE__); 
    unshift @INC, "$dir/../../../lib";
    unshift @INC, "$dir/../lib";
    push @INC, "$dir/../../Jifty/deps";
}

use Jifty::Script;
Jifty::Script->dispatch();
