<%init>
my $top = Jifty->web->navigation;
$top->child(Home       => url => "/", sort_order => 1);
if (Jifty->config->framework('AdminMode')) {
    $top->child(AdminMode       => url => "/__jifty/admin/", label => "Administration", sort_order => 999);
}
return();
</%init>
